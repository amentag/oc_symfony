{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}
    <h3>Formulaire de catégory</h3>

    {# On laisse vide la vue pour l'instant, on la comblera plus tard
       lorsqu'on saura afficher un formulaire. #}
    <div class="well">
        {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit" formnovalidate>Enregitrer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
